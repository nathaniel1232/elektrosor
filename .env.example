# Admin dashboard credentials
STUDIO_PASSWORD=elektrosor
STUDIO_SESSION_SECRET=generate-with-openssl-rand-hex-32

# Sanity CMS (optional - without this, job listings use fallback data)
NEXT_PUBLIC_SANITY_PROJECT_ID=your-sanity-project-id
NEXT_PUBLIC_SANITY_DATASET=production

# OpenAI API (optional - chatbot works without it)
OPENAI_API_KEY=sk-your-openai-api-key

# Installer API (if using installer system)
INSTALLER_API_KEY=your-installer-api-key
